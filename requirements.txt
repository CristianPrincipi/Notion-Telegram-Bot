# Minimum-version floors rather than tight pins, so an older build-image Python
# doesn't reject the very latest releases.

python-telegram-bot[job-queue]>=21.0
# The official SDK, not hand-rolled requests calls: it owns the retry/backoff on
# 429/5xx/529 that the three copied Anthropic calls never had, and it reads the
# `retry-after` header, which a hand-written `2 ** attempt` cannot. Pure Python
# over httpx — nothing to compile, so it is safe for Railway's build image.
anthropic>=0.69
requests>=2.31
pytz>=2024.1
PyPDF2>=3.0
# Article extraction. trafilatura scores the DOM for content density, so the
# summariser gets the article body instead of the body plus the nav, the cookie
# banner and the footer. beautifulsoup4 stays as the fallback parser — pure
# Python, and the two disagree often enough on odd layouts to be worth keeping
# both (services/learn.py).
#
# THIS DEPENDENCY WAS PREVIOUSLY REJECTED, and the reason is worth recording
# because it was a good reason at the time: trafilatura pulls lxml, lxml built
# from C source, and Railway's build image has no libxml2-dev / libxslt-dev, so
# the deploy failed. lxml ships manylinux wheels now (6.1.1), so nothing
# compiles. Verified before adding, not assumed:
#
#   pip install --dry-run --only-binary=:all: \
#       --platform manylinux_2_17_x86_64 --python-version 312 \
#       --target /tmp/dryrun -r requirements.txt
#
# --only-binary=:all: fails if ANY package in the tree lacks a wheel, so a clean
# exit is the proof. Re-run it before bumping either pin; if it ever goes red,
# that is the Railway build failing on your laptop instead of on Railway.
trafilatura>=2.0
beautifulsoup4>=4.12
google-api-python-client>=2.100
google-auth>=2.30
